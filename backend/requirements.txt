fastapi>=0.110
uvicorn[standard]>=0.29
python-multipart>=0.0.9
sqlalchemy>=2.0.25
pydantic>=2.6
PyJWT>=2.8
cryptography>=42.0
python-dotenv>=1.0

# AI / Agent stack
langchain>=0.2.5
langchain-core>=0.2.5
langchain-groq>=0.1.3
langgraph>=0.2.0
langchain-community>=0.2.5
langchain-text-splitters>=0.2.0

# Vector memory (local embeddings, no extra API key needed)
# NOTE: host CPU is Ivy Bridge (AVX1 only, no AVX2): onnxruntime MUST stay 1.16.x
#       and numpy must stay <2 (newer onnxruntime / torch builds require AVX2 and
#       hard-crash on load). chroma is used ONLY for its ONNX embedding function;
#       the vector store itself is pure-numpy (data/rag) because chroma/hnswlib
#       segfaults on this CPU.
chromadb>=0.5.5
onnxruntime==1.16.3
numpy<2

# Telegram
telethon>=1.36.0

# Document ingestion
pypdf>=4.0
docx2txt>=0.8
python-pptx>=0.6.23
Pillow>=10.0
pytesseract>=0.3.10

# Utilities
APScheduler>=3.10.4
aiofiles>=23.2